# Example Configs

Public configuration templates live in `config/*.example.yaml`.

Copy a template to a local `.yaml` file before use, then edit it for your own corpus and local indexes. Local `.yaml` files are ignored by git.

